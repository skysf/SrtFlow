# 视频轨：主轨与上层轨是对等的（画中画的取消）

> 2026-09-17 产品决策。改轨道模型、时间线行结构、轨道配色或预览点选之前必读。

## 决策

**取消「画中画」这个概念。** 上层视频轨不再是叠在角上的小窗，它就是**另一条
和主轨对等的视频轨** —— 内容一样铺满画面，差别只有叠放次序。

用户原话：「我希望也不要有画中画这个概念，就当作是另外一个类似于主轨道就行了。
然后内容也应该像主轨道那样是铺满屏幕的。」

## 已经对齐的

| 方面 | 现在 |
| --- | --- |
| 默认摆放 | 等比 contain 铺满画布、居中，与主轨**同一份账**（`defaultPlacement`，不再有 `isOverlay` 分叉） |
| 留空处 | 不补黑，露出下面那一层（导出侧不许给上层轨加 `pad`） |
| 行高 | **一轨一个值**（2026-09-22 改，见下节）。在那之前主轨和上层轨共用 `videoRowHeight`，拖一条一起动 |
| 图标 | 都是 `film`。上下叠放次序由行的位置表达，不需要第二种图标 |
| 颜色 | **一轨一色**（下节） |
| 画面渐变 | 任何轨都能设（`docs/architecture/video-fades.md`） |
| 菜单 | `Add to Upper Track…`（原 `Add Picture-in-Picture…`），落点仍是播放头 |

## 还没对齐的（第二步，尚未实施）

1. **轨内转场**：`setTransition` 只写 `state.mainClips`，导出只给主轨拼 xfade
   段，预览的接缝分派也只跑主轨。上层轨两段之间目前加不了转场，跨轨拖动会
   静默清掉 `transitionAfter`（`VideoEditTimelineEdits.relocateClip`、
   `VideoEditProject.toggleOverlay`）。
   技术前提已验证：`xfade` 在 rgba 上行为正确，alpha 走干净的线性斜坡
   （实测 255→204→127→76→0）。真正的工作量在于整条上层轨要改成「分节 →
   透明补隙（`color=black@0`）→ concat/xfade → 一次性 overlay」，音频同构。
2. **磁吸**：`packMain()` 只排主轨。已定口径是**每轨各自 pack**，磁吸开着时
   不保证轨间对齐。

> 单段的渐入渐出是**第一步就做掉**的折中：它解决了「上层轨满幅之后进出是全屏
> 硬切」这个最扎眼的问题，代价只有完整转场的两成。

## 轨道配色

**色号绑轨道身份，不绑行号。** 存在 `EditLane.colorIndex` 里，
`assignMissingTrackColors()` 在读盘和每次状态提交后补齐（幂等）。

- 补号规则：同类里取**最小未占用号**。删掉中间一条轨，活着的轨**不换色**；
  新开的轨捡回刚空出来的号，而不是一路往后漂到色相表尽头去撞色。
- 主轨恒定占用视频色号 0（它不是 `EditLane`，没地方存；而「最底下那条轨永远
  是这个颜色」本身可预期）。上层视频轨因此从 1 起补。
- 视频轨和音频轨**各占一段互不重叠的色相带**：先让人分清「这是画面还是声音」，
  再分清「第几条」。
- 色相表的顺序按「先分得最开」排，不是按色相递增 —— 常见工程只有两三条轨，
  前几个色号必须一眼分得清。
- 用在两处：时间线上块的填充、轨道头的色条。**色条不能省** —— 空轨在时间线上
  一个块都没有，只有它能说明那是哪条轨。预览里的选中框保持统一的青色，不跟
  轨道走（混在一起反而更难读）。

补号必须在**读盘**那一步做，不能等第一次编辑：`perform` 里的补号会和用户那次
改动一起进撤销栈，撤一下颜色就没了。

## 轨道头这一列（左侧 132pt）

一行四格，**每格宽度都写死**（`TimelineHeaderMetrics`）：色条 3 / 图标 16 /
推子 78 / 眼睛 14，没有色条、推子或眼睛的行也照样占着自己那一格。推子是 2026-09-23 加的
（整列从 54pt 宽到 132pt），最上面的标尺行放**总推子**，见 [推子与电平表](audio-mixer.md)。

> 为什么不能靠居中：`film` 比 `music.note` 宽、字幕行压根没有色条，各行的
> HStack 总宽因此不同，居中之后色条和眼睛的 x **一行一个样**（2026-09-18
> 用户报的「这一列要对齐」）。每格固定 → 每行总宽相同 → 居中即对齐，谁也不用
> 去量别人的位置。

这一列上有三个动作，互不打架：

| 动作 | 做什么 |
| --- | --- |
| 点**眼睛** | 整轨显隐。眼睛是 `Button`，它自己把点击吃掉，落不到行上 |
| 点**非眼睛**的地方 | 选中这一行的全部素材（2026-09-18 用户拍板）。⌘/⇧ 点是整行并入 / 整行减掉 |
| 上下**拖** | 调**这一条**轨的行高（视频 / 音频轨才有）。`minimumDistance: 2`，没挪动的点击不会被它吃掉 |
| 拖**推子** | 调这条轨的音量（推子自己的手势，子视图优先，不会连带调行高或选中整行） |

点选的判据是**纯值**：`TimelineRowSelection.ids(for:in:)`（一行 → 该选中哪些
ID），落进 `EditSelection` 的那一步在 `VideoEditProject.selectRow`。分成两层是
因为 `VideoEditProject` 是 @MainActor 的 App 类型，自检编不动它，而下面这几条
边界只有断言守得住：

1. **一行只产出一类**（剪辑 / 形状 / 文字 / 字幕 cue）。点选的互斥规则
   （`EditSelection`）因此一条都没松，混选仍然只能从框选进来。
2. **隐藏的行返回空**。隐藏 = 不可编辑（`trackRow` / `subtitleRow` 都关了命中
   测试），轨道头是同一条合同的另一半 —— 否则用户能从轨道头选中一批在时间线上
   碰都碰不到的块，⌫ 一按就删了看不见的东西。
3. **空行也返回空，调用方据此什么都不做**。注意这不是「选中 0 个」：后者会把
   用户手上已有的选择抹掉，而他只是点空了一下轨道头。
4. **⌘/⇧ 是整行并入 / 整行减掉，不是逐个翻转**。逐个翻转的话，行里已经选中的
   那几块会被取消 —— 用户看到的是「加选反而少了几块」。

## 行高：一轨一个值（2026-09-22 产品决策）

原来行高是**一类一个值**（`videoRowHeight` / `audioRowHeight`，存 UserDefaults）：
拖一条音频轨，所有音频轨一起变高。用户要的是「每一个轨道的高度都独立可以调节」，
所以改成一轨一个值。

| 方面 | 结论 |
| --- | --- |
| 存在哪 | `TimelineRowHeights`，跟着工程文件走（与 `timeline` **平级**，不在时间线里）。为什么不进 `TimelineState`：见 [工程文件](video-edit-project-file.md) 第三节 |
| 键 | **轨道身份**（`EditLane.id`；主轨单开一个 case），不是行号 —— 同「色号绑轨道身份」那条 |
| 没调过的轨 | 回落到这一类的默认高度（视频 54 / 音频 34，`nil = 继承`，同 `colorIndex`） |
| 默认值 | 只从老键 `editVideoRowHeight` / `editAudioRowHeight` 读一次（让改过行高的老用户打开就是原样），**之后不再写回** —— 写回就等于「拖一条，没调过的轨跟着一起变」 |
| 可调范围 | 视频轨 28…200、音频轨 20…200（2026-09-23 从 120 / 100 抬上来：立体声波形拆两条、在波形上拖音量曲线都要够高的轨）。**只有视频轨和音频轨可调** |
| 撤销 | **不进撤销栈**、不重建预览。行高是装饰状态，调完按 ⌘Z 该撤掉的是用户上一次真编辑 |

**字幕 / 文字 / 形状 / 滤镜行为什么不放开**：它们的行高和块高是一对硬编码常量
（字幕行 22 而 cue 块 14、形状行 26 而块 20），框选的命中判据直接依赖那个差
（[拖动手势](timeline-drag-gestures.md)）—— 放开就得把那套几何重做一遍。

## 上层轨盖住主轨：正常语义，以及它的代价

上层段满幅不透明时，那段时间主轨就是看不见 —— 这是标准 NLE 的 V2 语义，不是
bug。连带一个必须补的洞：

**预览里点画面只会命中最上面那一段。** 所以 `ClipTransformCanvas.selectClip`
提供 **⌥ 点击在命中的那一摞里轮换**（到底再绕回最上面）。没有它，被盖住的主轨
在预览里**选不中**。做成轮换而不是「只往下一层」：三层以上照样够得着，也不用
记按了几次。提示只在真有东西叠着（`visibleClips.count > 1`）时才挂 —— 平时在
预览里拖框的人不需要每次悬停都弹一条。

## 没有迁移

这是一次**语义换代**，工程格式升到 v10，**不做向下兼容的迁移**（用户明确选的
「干净改」）。`placement == nil` 的含义从「角落小框」变成「铺满画布」，
`overlayFraction`/`overlayAnchor`/`OverlayAnchor` 连同导出的九宫格表达式全部
删除。老工程读得进来（`decodeIfPresent` 会忽略多出来的键），但里面没手动摆过
的上层段会**变成铺满** —— 这是决策本身，不是缺陷。

反过来的方向由版本闸门挡住：只认 v9 的旧版打开新工程会把上层轨画回角落小窗，
成片当场不一样，所以 `requiresFormatVersion10` 无条件为真。

## 回归

| 守什么 | 在哪 |
| --- | --- |
| 上层轨铺满、两侧露出主轨（真导出四点像素探针 + 滤镜图无九宫格痕迹） | `scripts/check-video-fade.sh` |
| 同一场景的预览侧（模型摆放断言 + 真取帧亮度） | `scripts/check-preview-composition.sh` |
| 色号补号规则、删轨不换色、存盘往返、老工程读盘补齐 | `scripts/check-project-file.sh` |
| 轨道头整行点选的判据（一行一类 / 隐藏行 / 空行 / ⌘ 加选） | `scripts/check-project-file.sh` |
| 轨道头四格固定宽度、点选只走 `selectRow`、眼睛仍是 Button、推子占格与拖动中不写 state | `checks/timeline-drag-wiring.sh` |
| 行高的夹紧区间、键绑轨道身份（删轨不继承）、存盘往返与按需写键 | `scripts/check-project-file.sh` |
| 行高按轨道身份接线、起手值认轨、写入不进撤销栈 | `checks/timeline-drag-wiring.sh` |

**人工回归清单**（自动化够不着，发版前实机验证）：

- [ ] ⌥ 点击在三层叠放上轮换，到底绕回最上面；不按 ⌥ 永远选最上面那一段。
- [ ] 轨道头色条在空轨上也看得见；拖轨道头调行高时**只有被拖的那一条**变高，
      主轨、别的上层轨、别的音频轨都不动。
- [ ] 两条轨调成不同高度 → 存盘重开 → 各自的高度都还在；⌘Z 撤销的是上一次真
      编辑，不是刚才那次调行高。
- [ ] 轨道头的色条、图标、推子、眼睛四列上下**对齐**（视频行和音频行、字幕行一起看；
      标尺那一行的总推子和下面的推子对齐）。
- [ ] 在推子上拖：只调音量，不会调行高、也不会选中整行。
- [ ] 点轨道头非眼睛的地方选中整条轨的素材；点眼睛只切显隐、不选中；⌘ 点是加选，整行都选中时再 ⌘ 点是整行取消。
- [ ] 在轨道头上下拖仍然只调行高，不会顺手把整条轨选中。
- [ ] 新建/删除上层轨时，其余轨的颜色不变。
