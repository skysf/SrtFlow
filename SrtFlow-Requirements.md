# SrtFlow — 产品需求说明

> 根据前期讨论整理。目标：面向 Mac M 系列芯片的字幕转换与编辑工具，开源分发。

---

## 1. 产品定位

| 项 | 内容 |
|----|------|
| 名称 | **SrtFlow** |
| 平台 | macOS（Apple Silicon / M 系列） |
| 最低系统 | macOS 15 Sequoia 起（录屏需要 ScreenCaptureKit）|
| 形态 | **文档型**应用（打开文件 → 查看/编辑 → 保存） |
| 分发 | 本地自用优先，最终提供 **DMG**；之后可分享给朋友 |
| 开源 | 上传 GitHub 开源；**不包含个人信息与私密信息** |
| 界面语言 | **中英双语** |

---

## 2. 支持格式

- Text（`.txt`）
- SRT（`.srt`）
- VTT（`.vtt`）
- ASS（`.ass`）
- SSA（`.ssa`）

### 2.1 相互转换

| 源 → 目标 | Text | SRT | VTT | ASS | SSA |
|-----------|------|-----|-----|-----|-----|
| Text | — | ✓ | ✓ | ✓ | ✓ |
| SRT | ✓ | — | ✓ | ✓ | ✓ |
| VTT | ✓ | ✓ | — | ✓ | ✓ |
| ASS | ✓ | ✓ | ✓ | — | ✓ |
| SSA | ✓ | ✓ | ✓ | ✓ | — |

- 支持 **一次转换多个文件**（批量）
- ASS / SSA：**尽可能保留样式**（字体、颜色、位置、特效等）；内部编辑时尽量不丢样式信息

---

## 3. Text 输入规则

Text **可能带时间，也可能不带时间**。至少支持这两种常见写法：

**写法 A（SRT 风格时间轴）：**

```text
00:00:01,000 --> 00:00:03,500
你好，世界
```

**写法 B（方括号时间戳）：**

```text
[00:00:01.000] 你好，世界
```

**无时间戳时：**

- 导入后时间统一为 `00:00:00`（或起止均为 0）
- **由用户手动修改**时间，不做自动按时长拆分

---

## 4. 核心功能

### 4.1 打开与查看

- 打开 Text / SRT / VTT / ASS / SSA
- 清晰展示：
  - 序号
  - 时间码（开始 / 结束）
  - 字幕文本
  - （ASS/SSA）样式相关信息尽量可见/可保留

### 4.2 编辑与保存

- 修改时间码
- 修改字幕文字
- 增删字幕条目
- 直接编辑后保存
- 另存为其他格式（导出/转换）

### 4.3 视频同步预览

- 可选关联视频文件
- 选视频后与字幕一起播放预览
- 播放时能对照当前字幕条目

### 4.4 批量转换

- 一次拖入 / 选择多个文件
- 选择目标格式后批量导出
- 与单文档编辑流程分开，互不抢交互

### 4.5 启动入口与窗口结构（单窗口 + 侧边栏）

启动后不再要求先选字幕文件 —— 很多时候进来只是想把某个视频压小。

**一个主窗口装下三个工具**：左侧边栏三栏（压缩视频 / 烧制字幕 / 批量转换），
点一下就切；侧边栏底部常驻引擎状态和语言选择。记住上次用的那一栏，下次打开
直接进去。

- 三个工具的状态（两个编码队列、批量转换列表、烧字幕的样式与预览）全部提到
  **应用级共享对象**。切走那一栏时视图会被销毁，状态不能跟着走，否则正在跑的
  压缩会被中断。压缩因此可以在后台一直跑，用户同时去另一栏改字幕样式；
  侧边栏那一行会显示进度角标。
- **编辑字幕另开文档窗口**。它是 `DocumentGroup`，SwiftUI 规定文档场景各开自己
  的窗口，塞不进侧边栏的右侧面板；而且这样才保得住原生文档行为 —— ⌘S、另存为、
  恢复、版本历史、同时打开多个文件。侧边栏里那一行带一个「另开窗口」的角标。
- 支持把文件拖到窗口任意位置，按类型自动分流（视频 → 压缩；视频+字幕 →
  烧字幕；字幕 → 编辑）。视频也可以从 Finder 用「打开方式」交给 SrtFlow，
  会自动切到压缩那一栏并排队。
- 菜单：⌘1 / ⌘2 / ⌘3 切三栏。

### 4.6 字幕烧制（硬字幕）

把字幕永久合成进画面，压制前可调样式：

| 项 | 可调内容 |
|----|---------|
| 字体 | 字体族、字号、粗体、斜体、字间距 |
| 颜色 | 文字色、描边色、阴影色（均含透明度） |
| 描边 | 描边宽度、阴影偏移；或改用「文字底色条」 |
| 位置 | 九宫格对齐、距边缘距离、左右边距 |

- **预设**：内置白字黑边、黑字白边、黄字黑边、白字加投影、白字半透明底条；
  全部可自由调节，调好的样式可存成自定义预设。
- **字体列表只列渲染器真能加载的字体**，并标注是否含中文字形。macOS 的苹方等
  系统中文字体放在受保护目录里、渲染器打不开，若不过滤，用户选了苹方却会被
  悄悄替换成别的字体。选中的字体文件会软链进任务目录并用 `fontsdir` 锁定。
- **字体要能边挑边看**：列表里每个字体名都用它自己的字体绘制，并且是一个带
  选中态的真列表 —— 方向键走一遍，预览就跟着一句句变，不必「选中 → 关掉菜单 →
  等重渲染」才知道长什么样。列表带搜索，含中文字形的分在一组并加「中」标记。
- **尺寸以 1080p 为基准**：生成的 ASS 固定写 `PlayResY: 1080`，由渲染器等比
  缩放到实际分辨率，因此同一套样式在 720p 和 4K 上占画面比例完全一致。
- **预览分两种，各管一件事**：
  - **播放**：AVPlayer 放真实视频，字幕由 App 即时叠在画面上。用来核对
    「字幕跟画面/说话对不对得上」—— 静帧看不出时间轴问题。字幕这一层是
    近似渲染（SwiftUI 画不了文字描边，用八方向副本模拟），界面上明确写了这点。
    按字幕条目步进会把播放头跳到那一句的开头。
  - **精确帧**：用**与导出完全相同的滤镜链**让 ffmpeg 渲染真实的一帧，
    改动样式后防抖 250ms 重渲染。外观以这一张为准。
  - 两种都可以放大到铺满窗口（放大时自动收起侧边栏，Esc 还原，⌃⌘F 进系统
    全屏）。放大用 `overlay` 而不是 ZStack 兄弟节点 —— 后者会让里面的
    GeometryReader 把窗口一路撑大。
- **边距参考线**：拖动「距边缘距离 / 左右边距」滑块时，在预览上画出这两条边距的
  确切位置（左右两条竖线 + 一条横线，带数值），松手 3 秒后淡出。光看数字想象
  不出边距在哪儿，而左右边距还决定长句在哪里换行。
- 批量：按文件名自动配对 `video.mp4` ↔ `video.srt`（也认 `video.zh.srt`
  这类多一段后缀的写法），整批共用一套样式。

### 4.7 视频压缩

在几乎不损失画质的前提下大幅减小体积。两档编码：

| 档位 | 说明 |
|------|------|
| **H.264 CRF**（默认） | `libx264 -crf 23 -preset slow`。CRF 是恒定质量：复杂画面多给数据、简单画面少给，比固定码率省得多。23 是公认的「视觉无损」平衡点，滑杆范围 16–30；preset 越慢体积越小、画质不变 |
| **硬件极速** | `h264_videotoolbox`，走 M 系列芯片的媒体引擎，约 10 倍实时、几乎不耗电；质量 1–100，同体积画质略逊于 x264 slow |

其他导出选项：

- 分辨率上限（2160p/1440p/1080p/720p/480p）与帧率上限（60/30/24），**只降不升**
- 音频默认 `-c:a copy` 保留原始音质；mp4 装不下的格式（PCM、AC-3）自动转 AAC；
  也可手动指定 AAC 码率
- `-movflags +faststart`（网页边下边播）、剥除源元数据
- 烧字幕时可额外附加一条可开关的 mov_text 软字幕轨
- 统一输出 H.264 + yuv420p，保证微信、B 站、YouTube 与老设备都能播
- 批量队列（串行执行），逐个显示进度、速度、剩余时间、原始→输出体积与节省比例
- 界面上显示等效的 ffmpeg 命令，可直接复制到终端

### 4.8 界面语言

中英双语。可跟随系统，也可在应用内随时切换（首页底部下拉，或「设置…」⌘,）。
应用自身文字立即切换；菜单栏等 AppKit 部件下次启动生效。

### 4.9 字幕生成与翻译（本机）

从视频/音频直接生成字幕：用系统 SpeechAnalyzer 本机转写出原文轨（canonical），
可再用系统 Translation 生成译文轨（companion）；两轨在双轨编辑器里对照编辑，
按导出矩阵分别导出。全程在本机完成，不上传音视频。能力按 macOS 15/26 分层
（全局最低系统已提升到 15，翻译自基线即可用；语音生成需 26+），
缺模型时给出明确的安装与降级路径。设计与实现细节见
[docs/plans/2026-08-06-native-subtitle-generation.md](docs/plans/2026-08-06-native-subtitle-generation.md)。

---

## 5. 文档模型（高自由度设计）

原则：**怎么灵活怎么设计**。约定如下：

1. **文档主体 = 一份字幕文件**
   可独立打开、编辑、另存为 Text / SRT / VTT / ASS / SSA。

2. **视频为可选关联**
   - 用于同步预览
   - 不强制绑定
   - 可随时更换或移除

3. **路径记忆（可选增强）**
   - 可记住最近打开的字幕 / 视频路径，方便下次继续

4. **批量转换独立入口**
   - 多文件拖入 → 选目标格式 → 导出
   - 不与单文档编辑共用同一套主交互

---

## 6. 技术与实现（已定）

| 项 | 结论 |
|----|------|
| 技术栈 | **SwiftUI 原生 Mac App**（纯代码，无 storyboard / xcodeproj） |
| 工具链 | **仅 Command Line Tools**（Swift 6，含完整 macOS SDK 与 SwiftUI），**不需要完整 Xcode** |
| 构建 | SwiftPM：`swift build -c release --arch arm64`（仅 Apple Silicon，单架构保持小体积） |
| 代码结构 | `SrtFlowCore`（解析/序列化 + 编码参数与样式模型，纯逻辑可独立测试）+ `SrtFlow`（SwiftUI App 壳）+ `SrtFlowCoreChecks`（自检） |
| 测试 | CLT 不含 XCTest，核心库用 `swift run SrtFlowCoreChecks` 自检（**290 项断言**）；另有工程存盘、预览合成、导出 alpha 合成三套 `scripts/check-*.sh`，清单见 AGENTS.md |
| 视频引擎 | **随包自带原生 arm64 ffmpeg**（含 libass/libx264/VideoToolbox），放 `Contents/Helpers/ffmpeg`，以独立进程调用；`scripts/vendor-ffmpeg.sh` 负责下载 + SHA-256 校验 + 能力检测。包内没有时回退 `/opt/homebrew` → `/usr/local` → `PATH`，并在找到的是转译版或缺 libass 时提示 |
| 媒体信息 | 用 **AVFoundation** 读时长/分辨率/帧率/编解码器（正确处理竖拍旋转），因此不必再带 49 MB 的 ffprobe；AVFoundation 打不开的容器（mkv 等）退回解析 `ffmpeg -i` 的输出 |
| 打包 | `scripts/build-app.sh`：手写 `Info.plist` 组装 `.app` → 拷入 ffmpeg → **先签嵌套二进制再签外层** → `hdiutil` 生成 DMG |
| 图标 | `packaging/icon-source-1024.png` 是设计好的 1024 图；`scripts/make-icon.swift` 把里面那个圆角方块按量出来的几何（834×834 @ (94,87)、圆角 200）搬进标准栅格（824×824 @ 100，四周透明）并按圆角裁切 —— 源图**没有透明通道**，圆角外是近白底色，直接用会在 Dock 里显示成一个白方块。源图缺失时退回用代码画的那版 |
| 首次打开 | 走 **系统设置 → 隐私与安全性 → 仍要打开**，不需要终端。放行后 App 启动时会清掉自身包上的 `com.apple.quarantine`（`Quarantine.repairOwnBundleIfNeeded`），包内 ffmpeg 才能执行 —— 实测带着隔离标记时它跑不起来。仍然失败时界面上给出 `xattr -dr` 命令兜底，并带一个直接打开系统设置的按钮 |
| 体积/性能 | App 壳本体仍约 400 KB，加上随包 ffmpeg 后 `.app` 约 **50 MB**、DMG 约 **23 MB**。实测原生 arm64 比系统里的 x86_64（Rosetta）版快约 **2.1 倍**（30 秒 1080p，crf 23 + preset slow：14.7s vs 31.0s，输出体积相同） |
| 时间线捏合缩放 | **只能用 local NSEvent monitor**（`TimelineMagnificationBridge`），一切靠命中/响应链的方案真机必失败。2026-08-03 硬件实测定案；原因、失败方案清单与回归清单见 `docs/architecture/timeline-pinch-zoom.md` |
| 公证分发 | 自用/朋友分享可直接用（右键打开）；CLT 自带 `notarytool`，有 Developer ID 后可公证 |
| 授权 | 本体 **AGPL-3.0**（分发衍生作品须同样以 AGPL-3.0 开源）；随包 ffmpeg 为 GPLv2+，以独立进程调用、不做链接。分发时随包提供其许可与源码途径 |

---

## 7. 开源与隐私

- 代码开源，上传至 GitHub
- `.gitignore` 排除构建产物、密钥、个人配置等
- 不上传：个人路径中的隐私文件、账号密钥、证书、`.env`、本机私密配置等
- README / License 使用通用开源说明，避免写入个人敏感信息

---

## 8. 非目标（当前版本不做或未要求）

- 未要求必须上架 Mac App Store
- 未要求云同步 / 账号系统
- 无时间戳 Text **不做**自动按时长拆条（明确为手动改时间）
- 压缩**不做** HEVC 档与「指定目标体积」的两遍码率模式（评估后未选）
- 烧字幕不支持逐条不同样式 / 卡拉 OK 特效：整批统一一套样式，
  源文件里的 `{\...}` 覆盖标签会被清掉，保证界面上调的设置一定生效

---

## 9. 里程碑

1. ~~定技术栈~~ ✅ SwiftUI + SwiftPM + CLT 脚本打包（不依赖 Xcode）
2. ~~核心：解析 / 序列化 Text·SRT·VTT·ASS·SSA + 单文档编辑保存~~ ✅ 已实现并通过自检
3. ~~视频预览与条目联动~~ ✅ 已实现（关联视频、播放高亮当前条目、最近视频路径记忆）
4. ~~批量转换~~ ✅ 已实现（独立窗口，拖入多文件 → 选目标格式 → 导出）
5. ~~中英界面~~ ✅ 已实现（跟随系统语言）
6. ~~打包 DMG~~ ✅ `scripts/build-app.sh`
7. ~~GitHub 开源仓库（无个人信息）~~ ✅ 已完成源码隐私检查与开源文件准备
8. ~~精简启动首页~~ ✅ 四入口 + 整窗拖放分流，不再启动即要求打开字幕
9. ~~视频压缩（CRF / 硬件双档 + 批量队列）~~ ✅ 已实现并端到端验证
10. ~~字幕烧制（样式可调 + 预设 + 所见即所得预览 + 批量配对）~~ ✅ 已实现并端到端验证
11. ~~应用内中英切换~~ ✅ 已实现（首页下拉与「设置…」）

---

## 10. 待确认

字幕生成（§4.9）主体已实现并通过端到端冒烟，但仍有产品待拍板项（分段默认
阈值、生成后 cue 不自动跟随的取舍、告警图标呈现等），清单见
[docs/plans/2026-08-06-native-subtitle-generation.md](docs/plans/2026-08-06-native-subtitle-generation.md)
的 17.3。其余功能（视频压缩、字幕烧制、视频剪辑）已实现并通过真实素材端到端
验证；核心库自检 290 项全绿。
